# dataagent-benchmark

Standalone benchmark interface/API for evaluating data-curation agents fairly.

CLI:
- `dataagent-bench run <task.yaml> --agent module:Class`
- `dataagent-bench spec <task.yaml> --out bench_spec.json`
